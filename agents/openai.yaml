interface:
  display_name: "中文备注与安全扫描"
  short_description: "安装 Skill 或 MCP 后先三选一，再执行中文备注和或安全审计"
  default_prompt: "Use $chn-note-security-scan when installing a new skill or MCP. First ask the user whether to run Chinese annotation plus security audit, only Chinese annotation, or only security audit, then execute the chosen flow."
